---
applications:
  - name: webex-workspaces-((branch))
    routes:
      - route: webex-workspaces-((branch)).us-south.cf.appdomain.cloud
    memory: 512M
    buildpacks:
      - https://github.com/cloudfoundry/staticfile-buildpack