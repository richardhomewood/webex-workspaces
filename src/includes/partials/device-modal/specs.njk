{% macro specification(spec) %}
  <li>
    {% if spec.iconId %}
      {% icon spec.iconId %}
    {% endif %}
    <span>{{ spec.text }}</span>
  </li>
{% endmacro %}

{% macro render(common, specs) %}
  <div class="ws-device-specs ws-modal-section awaits-styling">
    <h6 class="ws-modal-section-heading awaits-styling">{{ common.deviceModal.specsHeadingText }}</h6>
    <ul>
      {% for spec in specs %}
        {{ specification(spec) }}
      {% endfor %}
    </ul>
  </div>
{% endmacro %}
