{% macro render(common, workspaceId, workspaces) %}
    <div class="swiper room-swiper">
        <div class="swiper-wrapper">
        {%- for room in workspaces[workspaceId].rooms %}

            <div class="ws-room-slide swiper-slide" id="{{room.slug}}-slide">
            <a href="#{{room.slug}}">{{ room.name }}</a>
            </div>
        {%- endfor %}
        </div>
    </div>
    <div class="shadowSwiper swiper">
        <div class="swiper-wrapper">
        {%- for room in workspaces[workspaceId].rooms %}
            <div class="ws-room-slide swiper-slide">
                <span>{{ room.name }}</span>
            </div>
        {%- endfor %}
        </div>
    </div>

{% endmacro %}
