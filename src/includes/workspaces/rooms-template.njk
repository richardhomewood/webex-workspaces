<!DOCTYPE html>
<main>
  <link rel="stylesheet" href="{% webpack 'main.css' %}">

  <div class="ws-root">

    <div class="ws-background-image">To be replaced with the background image for the current room.  And hotspots.</div>

    <a href="#">{% icon "site-logo" %}</a>

    <div class="ws-workspace-nav">
      <ul>
        {%- for workspace in workspaces %}
          <li><a href="#{{ workspace.route }}">{{ workspace.name }}</a></li>
        {%- endfor %}
      </ul>
    </div>

    <button class="ws-about-cta">{{ common.aboutWorkspaceText }}</button>

    <div class="ws-workspaces-rooms">
      <span class="ws-select-room-text">{{ common.selectRoomText }}</span>

      {# The main content template should iterate over room data to render them all here. #}
      {{ content | safe }}
    </div>

  </div>

</main>
<script async defer src="{% webpack 'main.js' %}"></script>
