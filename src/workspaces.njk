---
layout: workspaces/rooms-template.njk
---
<div class="ws-workspaces-container">
  {%- for workspace in workspaces %}
    <div class="ws-workspace" id="{{ workspace.slug }}Container">
      {%- for room in workspace.rooms %}
        <div class="ws-room" id="{{ workspace.slug }}-{{ room.slug }}">
          <div class="ws-room-thumbnail">
            <a href="#{{ workspace.slug }}-{{ room.slug }}">
              <div>To be replaced with an image</div>
              <div class="ws-room-label">{{ room.name }}</div>
            </a>
          </div>
        </div>
      {%- endfor %}
    </div>
  {%- endfor %}
</div>
